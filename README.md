# Notas:

este es mi primer programa en node

```
Options:
      --help      Show help                                            [boolean]
      --version   Show version number                                  [boolean]
  -n, --n1, --n1  Numero base de la tabla de multiplicar     [number] [required]
  -l, --listar    Valor true o false que muestra la tabla en consola
                                                      [boolean] [default: false]
  -h, --hasta     Valor rango de valores a multiplicar por la base
                                                           [number] [default: 1]
```